<settings xmlns="http://maven.apache.org/SETTINGS/1.0.0">
    <servers>
        <server>
            <id>github</id>
            <username>${env.GITHUB_TOKEN}</username> <!-- Changed from GH_TOKEN to GITHUB_TOKEN -->
            <password>${env.GITHUB_TOKEN}</password> <!-- Changed from GH_TOKEN to GITHUB_TOKEN -->
        </server>
    </servers>
    <profiles>
        <profile>
            <id>github</id>
            <repositories>
                <repository>
                    <id>github</id>
                    <url>https://maven.pkg.github.com/hadarkr60/Java-Artifacts</url>
                </repository>
            </repositories>
            <distributionManagement>
                <repository>
                    <id>github</id>
                    <url>https://maven.pkg.github.com/hadarkr60/Java-Artifacts</url>
                </repository>
            </distributionManagement>
        </profile>
    </profiles>
    <activeProfiles>
        <activeProfile>github</activeProfile>
    </activeProfiles>
</settings>
